export const getRuntimeConfig = () => ({
  /*
  endpoints: [
    example: {
      uri: 'endpoint.com',
      context: 'endpoint',
      headers: {
        key: 'name',
        bearer: process.env.ENDPOINT_BEARER_TOKEN
      },
    }
  ]
  */
});
